name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            echo "🚀 Conectado a la EC2, iniciando despliegue..."
            cd /home/ubuntu/ap-5b-backend

            git config --global --add safe.directory /home/ubuntu/ap-5b-backend

            echo "📦 Actualizando código desde main..."
            sudo git pull origin main

            echo "🧹 Deteniendo contenedores anteriores..."
            npm run docker-prod-down || true

            echo "🔧 Construyendo y levantando nuevos contenedores..."
            npm run docker-prod

            echo "🩺 Verificando estado de contenedores..."
            docker ps

            echo "✅ Despliegue completado con éxito en la instancia EC2."
